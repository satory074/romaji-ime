{
  "_comment": "Cloud-AI conversion settings. Copy to the per-user data dir and fill in your key. NEVER commit a real key. macOS: ~/Library/Application Support/RomajiIME/config.json   Windows: next to ime-server.exe (per its data dir).",

  "provider": "anthropic",
  "api_key": "sk-ant-...",
  "model": "claude-3-5-haiku-latest",

  "_openai_example": {
    "provider": "openai",
    "api_key": "sk-...",
    "model": "gpt-4o-mini"
  },
  "_gemini_free_tier_example": {
    "provider": "gemini",
    "api_key": "AIza... (free key from https://aistudio.google.com/apikey)",
    "model": "gemini-2.0-flash",
    "_note": "Uses Google's OpenAI-compatible endpoint automatically. Free tier inputs may be used by Google to improve products — avoid for sensitive text."
  },
  "_local_lmstudio_example": {
    "provider": "openai",
    "endpoint": "http://localhost:1234/v1",
    "model": "your-local-model"
  },

  "timeout_ms": 5000,

  "_behaviour": "auto_convert: convert automatically after a typing pause (Space always converts; Enter commits as-is). auto_convert_delay_ms: idle wait before that.",
  "auto_convert": true,
  "auto_convert_delay_ms": 500
}
